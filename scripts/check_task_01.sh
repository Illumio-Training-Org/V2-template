#!/bin/bash
# Validates Task 01. Exit 0 = pass, non-zero = fail (shown with the
# task's failure_message from tasks.hcl).

# [Replace with the real check, e.g.:]
# if grep -q "expected_value" /path/to/config; then
#   exit 0
# else
#   exit 1
# fi

exit 1
